# Todo App Test Fixture

This is a minimal test fixture for E2E tests of the projspec plugin.

## Purpose

- Provides a clean starting point for projspec plugin testing
- Tests will run `specify init` on a copy of this directory
- Simulates a fresh project without any projspec configuration

## Usage

E2E tests copy this fixture to a temporary directory before running test scenarios.
